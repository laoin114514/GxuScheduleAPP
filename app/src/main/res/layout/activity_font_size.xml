<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:fitsSystemWindows="true"
    android:background="@drawable/bg_page_gradient">

    <!-- Toolbar -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@android:color/transparent"
        app:navigationIcon="@drawable/ic_arrow_back"
        app:navigationIconTint="?attr/colorOnSurfaceVariant" />

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- 分区标题 -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="字体大小"
                android:textSize="@dimen/text_title"
                android:textStyle="bold"
                android:textColor="?attr/colorOnSurface"
                android:layout_marginBottom="6dp" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="字体与界面尺寸会一起变化，版面比例保持不变。"
                android:textSize="@dimen/text_body_small"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:layout_marginBottom="14dp" />

            <!-- 顶部预览：内容按当前档位实时渲染，拖动滑块时即时变化 -->
            <com.google.android.material.card.MaterialCardView
                style="@style/AppSectionCard"
                android:layout_marginBottom="18dp">

                <FrameLayout
                    android:id="@+id/preview_container"
                    android:layout_width="match_parent"
                    android:layout_height="140dp" />

            </com.google.android.material.card.MaterialCardView>

            <!-- 当前档位名称 + 倍率 -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="bottom">

                <TextView
                    android:id="@+id/tv_scale_value"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="标准"
                    android:textSize="@dimen/text_headline"
                    android:textStyle="bold"
                    android:textColor="?attr/colorOnSurface" />

                <TextView
                    android:id="@+id/tv_scale_factor"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="1.0×"
                    android:textSize="@dimen/text_body"
                    android:textColor="?attr/colorOnSurfaceVariant" />
            </LinearLayout>

            <!-- 档位滑块：五档离散取值，外观与课表页周数滑块一致（@style/AppSlider）；
                 实际取值范围由代码按 UiScaleManager.PRESETS 设置 -->
            <com.google.android.material.slider.Slider
                android:id="@+id/slider_scale"
                style="@style/AppSlider"
                android:valueFrom="0"
                android:valueTo="4"
                android:stepSize="1"
                android:value="2" />

            <!-- 刻度名称：两端与滑块首尾对齐，中间居中（0dp 权重四等分） -->
            <LinearLayout
                android:id="@+id/ll_scale_labels"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:paddingHorizontal="24dp" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="选择后立即生效。桌面小组件由系统桌面绘制，不受此设置影响。"
                android:textSize="@dimen/text_caption"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:layout_marginTop="18dp" />

        </LinearLayout>

    </androidx.core.widget.NestedScrollView>

</LinearLayout>
